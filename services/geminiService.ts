// This service has been deprecated as AI features were removed.
// import { GoogleGenAI } from "@google/genai";
// import { AIChatMessage, GroundingSource } from "../types";

export const searchAmenities = async (
  query: string,
  userLat: number,
  userLng: number
) => {
  return null;
};